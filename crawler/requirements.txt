seleniumbase>=4.27,<5.0
beautifulsoup4>=4.12,<5.0
requests>=2.31,<3.0
lxml>=5.0,<6.0
google-cloud-storage>=2.14,<4.0
# Note: NO pyvirtualdisplay. It SIGKILLs the Python process inside this
# Docker container at SB() launch (reproduced consistently). On Docker we
# use `SB(uc=True, headless=True)` (USE_XVFB=false in env) which doesn't
# need any virtual-display lib. Colab still uses xvfb mode via its own setup.
